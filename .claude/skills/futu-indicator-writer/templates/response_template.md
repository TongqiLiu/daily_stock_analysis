## 脚本代码

```txt
{PASTE_FUTU_FORMULA_HERE}
```

## 参数说明

- `N1`：含义，默认值，建议范围
- `N2`：含义，默认值，建议范围
- `M`：含义，默认值，建议范围

## 导入自检

1. 粘贴到富途指标编辑器后能保存成功，无语法报错。
2. 在最近 3-6 个月 K 线上，至少出现若干买卖信号（非 0 次）。
3. 信号位置符合预期：买点靠近低位/突破位，卖点靠近高位/转弱位。
4. 调整参数后信号密度有变化，说明参数生效。
5. 若信号过少，先降低过滤强度；若过多，增加趋势过滤。
