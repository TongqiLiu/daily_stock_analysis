# Nunu Wave Strategy / nunu波浪
# Trigger: 问股策略栏选择「nunu波浪」，或用户明确提到“nunu波浪/引导楔形/终结楔形/WXYXXZ”。

name: nunu_wave
display_name: nunu波浪
description: 基于“级别优先 + 结构自洽”的进阶波浪框架，重点识别引导楔形/终结楔形、WXYXXZ 复杂调整、主备计数切换与失效位。
category: framework
core_rules: [1, 2, 3, 4, 6]
required_tools:
  - get_realtime_quote
  - get_daily_history
  - analyze_trend
  - calculate_ma
  - get_volume_analysis
aliases: [nunu波浪, nunu, 波浪进阶, 楔形判别, WXYXXZ, 双锯齿]
default_priority: 81
user_invocable: true
default_active: false
default_router: false
market_regimes: [volatile, trending_down, trending_up]

instructions: |
  **nunu波浪（级别优先版）**

  你的任务是用“先级别、后结构、再信号”的顺序做波浪分析，避免被金叉/放量等短期信号误导。

  ---

  ## 一、工具调用顺序（必须）

  1. `get_realtime_quote`：当前价、涨跌幅、量比
  2. `get_daily_history`（days=260）：用于大级别计数
  3. `get_daily_history`（days=120）：用于中级别细分
  4. `analyze_trend`：MA/MACD/RSI/KDJ，作为结构验证
  5. `calculate_ma`：均线斜率和中期支撑阻力
  6. `get_volume_analysis`：放量/缩量、量价背离
  7. 可选补充：`analyze_pattern`（仅辅助，不得覆盖浪型判定）

  ---

  ## 二、硬规则（违反即失效）

  1. 推动浪中，第 3 浪不能是 1/3/5 三者最短。
  2. 第 2 浪不能跌破第 1 浪起点。
  3. 第 4 浪与第 1 浪价格区重叠需高度谨慎（斜三角例外，需单独说明）。
  4. 若某主计数导致上述任一规则被破坏，必须降级为备选计数。

  输出措辞规范：
  - 不要说“绝对不可能”。
  - 统一表述为“在当前级别与当前计数下不成立”。

  ---

  ## 三、级别优先与主备计数

  1. 先给出大级别主方向（推动中 / 调整中 / 末端衰竭）。
  2. 再给中级别主计数 + 备选计数（至少 1 个备选）。
  3. 只有当“级别位置 + 内部结构 + 前后浪型自洽”同时满足时，才升级为高置信主计数。
  4. 金叉、放量、单日大阳线只能做辅助确认，不能单独推翻浪型结构。

  ---

  ## 四、楔形判别（引导 vs 终结）

  ### 1) 引导楔形（Leading）
  - 常见位置：更大级别的 1 浪或 A 浪初段。
  - 含义：趋势“刚启动或中继”，后续通常还有同向大段。
  - 实战特征：局部看似反转，但全局目标位未达标；内部更偏推动节奏。

  ### 2) 终结楔形（Ending）
  - 常见位置：更大级别的 5 浪或 C 浪末端。
  - 含义：趋势衰竭，后续应快速反向。
  - 必要确认：上破楔形上沿后能站稳，且回踩不破，再走出低级别推动。

  ### 3) 判别优先级
  - 先看位置（级别目标是否达标）；
  - 再看内部结构（推动/调整属性）；
  - 最后看量价与指标（金叉/放量仅作辅助）。

  ---

  ## 五、复杂调整识别（WXY / WXYXZ / WXYXXZ）

  1. 急跌后的第一腿优先按 A 或 W 处理，后续默认进入调整框架。
  2. `C` 浪时间不超过 `A` 浪时间 10 倍仅作经验指引，不是硬规则。
  3. 若后续有效跌破 `Y` 点前低，优先否定三角/平台主计数，转向 `Z` 末腿框架。
  4. 双锯齿或多重组合中，`Z` 低点优先参考：
     - `W` 与 `Y` 的连线支撑；
     - 调整通道下沿；
     - 低级别背离/缩量止跌共振。

  ---

  ## 六、常见误判过滤

  1. “金叉 + 放量”不等于跌势结束。
  2. 看起来像新 1-2-3 的起涨段，若内部不是合格推动浪（更像锯齿三段），不得判大三浪启动。
  3. 若“终结楔形”解释与前序浪型冲突，先否定终结，保留引导或中继解释。

  ---

  ## 七、输出格式（严格）

  ### 1. 当前计数结论
  - 主计数：`[级别] [浪型位置]`
  - 备选计数：`[级别] [浪型位置]`
  - 置信度：高 / 中 / 低（并说明原因）

  ### 2. 关键判别依据（3-5 条）
  - 必须包含：级别位置、内部结构、与前序浪型一致性
  - 可补充：量价/指标共振或背离

  ### 3. 失效与确认位
  - 主计数失效位：`价格位 + 失效条件`
  - 备选转主触发位：`价格位 + 触发条件`
  - 若涉及楔形，必须写“上沿/下沿突破后的二次确认条件”

  ### 4. 交易执行建议（条件化）
  - 当前阶段：左侧试错 / 等确认 / 反弹减仓 / 趋势跟随
  - 仓位建议区间：0-20% / 20-40% / 40-60%
  - 风险点：至少 2 条（如“跌破 Z 低点”“假突破回落”）

  ---

  ## 八、评分调整建议（sentiment_score）

  - 明确第 2 浪末端企稳并未破坏规则：`+10`
  - 明确第 3 浪启动且结构完整：`+12`
  - 识别为第 5 浪末端或终结失败：`-8`
  - 识别为 C/Z 下行进行中：`-12`
  - 主计数被硬规则否定但仍强行看多：额外 `-6`（风险惩罚）
